"use client";
import Link from 'next/link';
import { Menu, LogOut, User, Home, Truck } from 'lucide-react';

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex min-h-screen bg-background text-foreground">
      {/* Sidebar */}
      <aside className="w-64 bg-primary/5 border-r border-border flex-shrink-0">
        <div className="flex flex-col h-full p-4">
          <div className="flex items-center space-x-3 mb-6">
            <div className="h-8 w-8 bg-primary/20 rounded-md flex items-center justify-center">
              <Menu className="h-4 w-4 text-primary" />
            </div>
            <h2 className="text-xl font-bold text-foreground">Matelematics</h2>
          </div>
          <nav className="mt-auto space-y-2">
            <Link href="/dashboard" className="flex items-center px-3 py-2 rounded-md text-sm font-medium hover:bg-primary/10">
              <div className="h-5 w-5 flex items-center justify-center">
                <Home className="h-4 w-4" />
              </div>
              <span className="ml-3">Dashboard</span>
            </Link>
            <Link href="/dashboard/vehicles" className="flex items-center px-3 py-2 rounded-md text-sm font-medium hover:bg-primary/10">
              <div className="h-5 w-5 flex items-center justify-center">
                <Truck className="h-4 w-4" />
              </div>
              <span className="ml-3">Vehicles</span>
            </Link>
          </nav>
          <div className="mt-auto pt-4 border-t border-border">
            <div className="flex items-center space-x-3">
              <div className="h-8 w-8 bg-primary/20 rounded-md flex items-center justify-center">
                <User className="h-4 w-4 text-primary" />
              </div>
              <div>
                <h3 className="font-medium text-foreground">John Doe</h3>
                <p className="text-sm text-foreground/60">john@example.com</p>
              </div>
            </div>
           <div className="mt-4">
  <button
    onClick={() => window.location.href = "/"}
    className="flex items-center w-full rounded-lg px-3 py-2 text-sm hover:bg-gray-100"
  >
    <LogOut className="h-4 w-4 mr-3" />
    <span>Déconnexion</span>
  </button>
</div>
          </div>
        </div>
      </aside>

      {/* Main Content */}
      <main className="flex-1 p-6">
        {children}
      </main>
    </div>
  );
}